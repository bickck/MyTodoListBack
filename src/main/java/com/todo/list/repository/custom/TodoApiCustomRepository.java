package com.todo.list.repository.custom;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import com.todo.list.repository.mapper.TodoMapper;

public interface TodoApiCustomRepository {

//	Page<TodoMapper> findRecommandTodosApi(Pageable pageable);

//	Page<TodoMapper> findDailyTodosApi(Pageable pageable);
}
