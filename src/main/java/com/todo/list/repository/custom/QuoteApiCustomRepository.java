package com.todo.list.repository.custom;

import java.util.List;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.repository.Repository;

import com.todo.list.entity.QuoteEntity;
import com.todo.list.repository.mapper.QuoteMapper;

public interface QuoteApiCustomRepository {

//	Page<QuoteMapper> dailyQuotesApi(Pageable pageable);
	
//	Page<QuoteMapper> recommandQuotesApi(Pageable pageable);
}
